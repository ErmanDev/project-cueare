# web

React + TypeScript admin and moderator UI (Vite). Same navy / red palette as the Flutter app.

```powershell
pnpm install
pnpm dev                 # http://localhost:5173  (proxies /api → :8080)
```

Production build is served by the API after `cd ../server && bun run build:web`.
